
<!-- BEGIN-UID.user_ctrls -->

<!-- END-UID.user_ctrls -->

<div  id="add_new_to_tasks" style="float:right; width:60%;">
    <% dest = url_for :controller => 'committee_roles', :action => 'update_tasks', :id => @committee_role.id %>
    <% form_tag( :controller => 'task_objs', :action => 'new', :redirect => dest ) do %>
        <%= submit_tag 'Add New...' %>
    <% end %> <!-- end form_tag -->
</div>

<div id="add_to_tasks_list">
    <% form_tag( :controller => 'committee_roles', :action => :update_tasks, :id => @committee_role ) do %>
        <%= submit_tag 'Add Selected' %>
        <p class="select_list">&nbsp;</p>
        <% for task_obj in @task_objs %>
            <p class="select_list"> <%= check_box_tag("task_obj_ids[]",task_obj.id) %> <%= task_obj.sname %> </p>
        <% end %>
        <p class="select_list">&nbsp;</p>
        <%= submit_tag 'Add Selected' %>
    <% end %> <!-- end form_tag -->
</div>

<!-- BEGIN-UID.user_divs -->

<!-- END-UID.user_divs -->

